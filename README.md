# Understanding-Ci-CD-pipeline-through-GitHub-Actions
A very simple Python script that does basic math. The Goal: Show the students that when we push good code, the pipeline passes (Green). When we push bad code, the pipeline fails and catches our mistake (Red).
